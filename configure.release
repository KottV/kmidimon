#!/bin/bash
# a typical configuration for production usage...
mkdir -p build
cd build
cmake .. \
    -DCMAKE_PREFIX_PATH=/usr/share/drumstick/cmake \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr
echo "You can run 'cmake --build .' now"
